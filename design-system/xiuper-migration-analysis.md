# AutoDev Xiuper 设计系统迁移方案

## 1. 执行摘要

本文档分析当前 AutoDev 设计系统与 Xiuper 设计愿景之间的差距，并提供详细的迁移方案。Xiuper 的核心理念是 **"唯快不破"**，通过"霓虹暗夜"与"气韵流动"的视觉语言，构建一种独特的 AI 开发环境体验。

### 核心目标
- 🎯 **感知性能优化**：消除等待焦虑，让 UI "永不静止"
- 🎨 **东方赛博朋克美学**：融合武侠气韵与科技感
- ⚡ **极速交互体验**：键盘优先，"Xiu" 曲线动效
- 🔍 **人机协作可视化**：清晰区分用户意图与 AI 响应

---

## 2. 当前设计系统分析

### 2.1 现有色彩系统 (AutoDevColors.kt)

**优势：**
- ✅ 完整的 10 级色阶体系（Indigo、Cyan、Neutral 等）
- ✅ 亮色/暗色双主题支持
- ✅ 语义化颜色（Success、Warning、Error、Info）
- ✅ 专用的 Syntax 高亮和 Diff 颜色
- ✅ 遵循 WCAG 无障碍标准

**与 Xiuper 的差距：**

| 维度 | 当前状态 | Xiuper 目标 |
|------|----------|-------------|
| 主色 | Intelligent Indigo (#4f46e5) | 电光青 Accent-Xiu (#00F3FF) |
| AI 意图色 | Cyan (#06b6d4) | 霓虹紫 Accent-AI (#D946EF) |
| 背景色 | Neutral-900 (#171717) | 虚空黑 bg-void (#0B0E14) |
| 强调色用途 | 通用强调 | 人机边界区分（用户 vs AI） |

**问题识别：**
1. ❌ 缺少 `accent-xiu` 和 `accent-ai` 双强调色系统
2. ❌ 背景色不够"深邃"，缺少冷色调偏移
3. ❌ 没有霓虹发光效果的色彩定义
4. ❌ 硬编码颜色仍然存在（189 处 `Color(0x...)`）

### 2.2 现有动效系统

**当前实现：**
```kotlin
// 使用标准 tween 和 LinearEasing
animationSpec = infiniteRepeatable(
    animation = tween(2000, easing = LinearEasing),
    repeatMode = RepeatMode.Restart
)
```

**与 Xiuper 的差距：**

| 维度 | 当前状态 | Xiuper 目标 |
|------|----------|-------------|
| 曲线类型 | 单一 LinearEasing | 三套专用曲线 |
| 响应速度 | 标准 2000ms | 极速 ease-xiu (~150ms) |
| 物理感 | 无 | spring-tactile 弹簧回弹 |
| 代码生成 | 无特殊处理 | ease-stream 线性流 |

**问题识别：**
1. ❌ 缺少 "Xiu" 曲线 `cubic-bezier(0.16, 1, 0.3, 1)`
2. ❌ 缺少弹簧动效 `spring-tactile`
3. ❌ 无骨架屏/呼吸光效的加载状态
4. ❌ 无动态排版（Kinetic Typography）实现

### 2.3 现有组件架构

**已有组件：**
```
compose/
├── agent/          # AI 代理面板、计划面板、任务面板
├── chat/           # 聊天界面、消息列表
├── editor/         # 编辑器、代码补全、Diff 视图
├── sketch/         # 代码块渲染、Diff 渲染、Thinking 块
├── terminal/       # 终端渲染、ANSI 解析
└── theme/          # 主题管理（AutoDevColors、AutoDevTheme）
```

**与 Xiuper 的差距：**

| 组件 | 当前状态 | Xiuper 特色需求 |
|------|----------|-----------------|
| 命令面板 | 基础下拉菜单 | 全知命令栏 Omnibar（模糊搜索、上下文芯片） |
| 代码生成 | 标准文本追加 | 幽灵文本 + 部分采纳 + 动力学排版 |
| Diff 视图 | 基础内联展示 | 熔炉差异视图 + YOLO 模式 |
| 加载状态 | 简单 Spinner | 骨架屏 + 呼吸光效 + 思维链可视化 |

### 2.4 TypeScript 设计系统

**当前实现：**
- `colors.ts` - 与 Kotlin 同步的色阶定义
- `theme-helpers.ts` - Chalk/Ink 语义化辅助

**问题识别：**
1. ❌ 缺少动效曲线的 CSS 变量定义
2. ❌ 无霓虹发光效果的样式
3. ❌ CLI 动效无法体现 Xiuper 美学

---

## 3. Xiuper 设计系统规范

### 3.1 新色彩体系

```kotlin
object XiuperColors {
    // ========================================================================
    // 虚空色阶 (The Void Scale) - 带微量蓝色的冷色调
    // ========================================================================
    object Void {
        val bg = Color(0xFF0B0E14)       // 全局底层背景
        val surface1 = Color(0xFF151922) // 侧边栏、面板
        val surface2 = Color(0xFF1F2430) // 悬停、输入框、代码块
        val surface3 = Color(0xFF2A3040) // 边框、分割线
        val overlay = Color(0x800B0E14)  // 模态遮罩 (50% opacity)
    }
    
    // ========================================================================
    // 能量色阶 (The Energy Scale) - 人机边界
    // ========================================================================
    object Energy {
        val xiu = Color(0xFF00F3FF)      // 电光青 - 用户意图
        val xiuHover = Color(0xFF33F5FF)
        val xiuDim = Color(0x4000F3FF)   // 30% opacity - 光晕
        
        val ai = Color(0xFFD946EF)       // 霓虹紫 - AI 生成
        val aiHover = Color(0xFFE066F5)
        val aiDim = Color(0x40D946EF)    // 30% opacity - 光晕
    }
    
    // ========================================================================
    // 信号色阶 (Signal Colors)
    // ========================================================================
    object Signal {
        val success = Color(0xFF00E676) // 高亮绿
        val error = Color(0xFFFF1744)   // 高亮红
        val warn = Color(0xFFFFEA00)    // 赛博黄
        val info = Color(0xFF2196F3)    // 信息蓝
    }
}
```

### 3.2 动效曲线定义

```kotlin
object XiuperAnimation {
    // "Xiu" 曲线 - 极速响应，瞬间到达
    val EaseXiu = CubicBezierEasing(0.16f, 1f, 0.3f, 1f)
    
    // 线性流 - AI 代码生成的稳定输出
    val EaseStream = LinearEasing
    
    // 武侠弹簧 - 微交互的触觉反馈
    val SpringTactile = spring<Float>(
        dampingRatio = 0.6f,
        stiffness = 180f
    )
    
    // 标准时长
    object Duration {
        const val INSTANT = 100    // 瞬间反馈
        const val FAST = 150       // 快速过渡
        const val NORMAL = 250     // 标准过渡
        const val SLOW = 400       // 强调动画
    }
}
```

### 3.3 间距系统 (4px Grid)

```kotlin
object XiuperSpacing {
    val space1 = 4.dp   // 图标与文本间距
    val space2 = 8.dp   // 按钮内部 padding，紧凑列表
    val space3 = 12.dp  // 卡片内部元素间距
    val space4 = 16.dp  // 标准布局间距
    val space6 = 24.dp  // 容器边缘 padding
    val space8 = 32.dp  // 模块间距
}
```

---

## 4. 迁移方案

### Phase 1: 基础设施升级 (Week 1-2)

> **注意**: Xiuper 是 AutoDev 的特殊版本，不新建独立文件，而是增强现有设计系统。

#### 4.1.1 增强 AutoDevColors.kt

**任务：**
- [ ] 在 `AutoDevColors` 中添加 `Xiuper` 子对象（Void、Energy、Signal 色阶）
- [ ] 更新 `AutoDevTheme.kt` 添加 Xiuper 主题模式
- [ ] 保持向后兼容性

**文件变更：**
```
mpp-ui/src/commonMain/kotlin/cc/unitmesh/devins/ui/compose/theme/
├── AutoDevColors.kt (增强，添加 Xiuper 子对象)
├── AutoDevTheme.kt (增强，添加 Xiuper 主题)
└── AutoDevAnimation.kt (新建 - 动效曲线定义)
```

#### 4.1.2 创建动效系统与启动动画

**任务：**
- [ ] 在 `AutoDevAnimation.kt` 中定义 Xiuper 动效曲线
- [ ] **实现 Launch 启动动画** - 展示 Xiuper 的 "咻" 美学
- [ ] 创建可复用的动效扩展函数
- [ ] 添加 `prefers-reduced-motion` 支持

**Launch 动画规格：**
```kotlin
// 启动时的品牌动画
// 1. Logo 从中心快速放大 (ease-xiu)
// 2. 电光青拖尾效果
// 3. 文字 "Xiuper Fast" 滑入
// 4. 整体淡出进入主界面
```

#### 4.1.3 TypeScript 同步

**任务：**
- [ ] 更新 `colors.ts` 添加 Xiuper 色彩
- [ ] 创建 `animation.css` 定义 CSS 变量
- [ ] 更新 `theme-helpers.ts`

---

### Phase 2: 核心组件改造 (Week 3-4)

#### 4.2.1 幽灵文本组件 (Ghost Text)

**文件：** `GhostTextRenderer.kt` (新建)

**特性：**
- AI 预测文本以 `accent-ai` 颜色显示（40% 透明度 + 斜体）
- Tab 键全量采纳
- Ctrl+→ 逐词采纳
- 采纳时 `spring-tactile` 弹跳效果

#### 4.2.2 全知命令栏 (Omnibar)

**文件：** `CommandOmnibar.kt` (重构自现有命令面板)

**特性：**
- 宽幅悬浮设计（750px+）
- 高斯模糊背景 `backdrop-filter: blur(20px)`
- 上下文芯片显示当前位置
- 模糊搜索 + 快捷键提示
- 预览面板

#### 4.2.3 熔炉差异视图 (Forge Diff)

**文件：** `DiffSketchRenderer.kt` (改造)

**新增特性：**
- 智能视图切换（内联 vs 并排）
- 字符级差异高亮
- YOLO 极速模式（直接应用 + 撤销条）
- 撤销时的"倒带"动画

---

### Phase 3: AI 交互升级 (Week 5-6)

#### 4.3.1 思维链可视化

**文件：** `ThinkingBlockRenderer.kt` (改造)

**新增特性：**
- 呼吸边框（品红色光晕）
- 推理步骤折叠 Accordion
- 骨架屏占位（动态长度预估）

#### 4.3.2 动力学排版

**文件：** `StreamingTextRenderer.kt` (新建)

**特性：**
- 块级流式渲染（语义块为单位）
- 滑入动效（5px 位移 + 透明度）
- 线性匀速输出

#### 4.3.3 光标残影

**文件：** 编辑器光标增强

**特性：**
- 快速跳跃时的青色拖尾
- 代码生成时的运动模糊效果

---

### Phase 4: 清理与优化 (Week 7-8)

#### 4.4.1 硬编码颜色清理

**当前状态：** 189 处硬编码 `Color(0x...)`

**任务：**
- [ ] 批量替换为设计系统 Token
- [ ] 重点文件：
  - `DiffSketchRenderer.kt` (8 处)
  - `PlanSummaryBar.kt` (12 处)
  - `TaskPanel.kt` (5 处)
  - `AnsiParser.kt` (32 处 - 终端颜色可保留)

#### 4.4.2 性能优化

**任务：**
- [ ] 确保所有动效仅操作 `transform` 和 `opacity`
- [ ] 添加 `reduced-motion` 媒体查询支持
- [ ] 懒加载大型组件

---

## 5. Xiuper 特色组件清单

以下组件应当体现 **AutoDev Xiuper** 的独特美学和交互理念：

### 5.1 必须 Xiuper 化的组件

| 组件名 | 特色要求 | 优先级 |
|--------|----------|--------|
| **全知命令栏** | 模糊搜索、上下文芯片、预览面板、`ease-xiu` 动效 | P0 |
| **幽灵文本** | 双色区分（用户/AI）、部分采纳、弹簧反馈 | P0 |
| **思维链面板** | 呼吸光晕、骨架屏、步骤流出动画 | P0 |
| **流式代码生成** | 块级渲染、滑入动效、动力学排版 | P1 |
| **熔炉 Diff** | 智能切换、字符级高亮、YOLO 模式、倒带动画 | P1 |
| **计划面板** | 状态指示器升级为能量色、进度动效 | P2 |
| **终端渲染** | ANSI 颜色映射到 Xiuper 色阶 | P2 |

### 5.2 保持现有设计的组件

| 组件名 | 理由 |
|--------|------|
| 设置对话框 | 功能性 UI，无需强调美学 |
| 文件树视图 | 标准交互，保持一致性 |
| 表单控件 | 沿用 Material 3 规范 |

---

## 6. CSS 变量规范

为 TypeScript/CSS 实现提供的变量定义：

```css
:root {
  /* 虚空色阶 */
  --xiuper-void-bg: #0B0E14;
  --xiuper-void-surface-1: #151922;
  --xiuper-void-surface-2: #1F2430;
  --xiuper-void-surface-3: #2A3040;
  --xiuper-void-overlay: rgba(11, 14, 20, 0.8);
  
  /* 能量色阶 */
  --xiuper-xiu: #00F3FF;
  --xiuper-xiu-hover: #33F5FF;
  --xiuper-xiu-dim: rgba(0, 243, 255, 0.25);
  --xiuper-ai: #D946EF;
  --xiuper-ai-hover: #E066F5;
  --xiuper-ai-dim: rgba(217, 70, 239, 0.25);
  
  /* 信号色 */
  --xiuper-success: #00E676;
  --xiuper-error: #FF1744;
  --xiuper-warn: #FFEA00;
  --xiuper-info: #2196F3;
  
  /* 动效曲线 */
  --ease-xiu: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-stream: linear;
  --spring-tactile: linear(
    0, 0.009, 0.035 2.1%, 0.141 4.4%, 0.723 12.9%, 0.938 16.7%,
    1.017 18.4%, 1.077 20.4%, 1.121 22.8%, 1.149 25.7%, 1.159 29.6%,
    1.14 34.9%, 1.079 43%, 1.04 48.3%, 1.016 53.4%, 0.999 61.6%,
    0.995 71.2%, 1
  );
  
  /* 时长 */
  --duration-instant: 100ms;
  --duration-fast: 150ms;
  --duration-normal: 250ms;
  --duration-slow: 400ms;
  
  /* 间距 */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
}
```

---

## 7. 霓虹发光效果

```css
/* 焦点光环 */
.xiuper-focus-ring {
  box-shadow: 
    0 0 0 2px var(--xiuper-void-bg),
    0 0 0 4px var(--xiuper-xiu),
    0 0 15px 2px var(--xiuper-xiu-dim);
  transition: box-shadow 0.15s var(--ease-xiu);
}

/* AI 思考光晕 */
.xiuper-ai-pulse {
  animation: ai-breathe 2s ease-in-out infinite;
}

@keyframes ai-breathe {
  0%, 100% { box-shadow: 0 0 0 0 var(--xiuper-ai-dim); }
  50% { box-shadow: 0 0 20px 4px var(--xiuper-ai-dim); }
}
```

---

## 8. GitHub Issues 建议

根据上述分析，建议创建以下 GitHub Issues：

### Issue 1: [Design System] 增强 AutoDevColors 支持 Xiuper 主题
**标签:** `enhancement`, `design-system`, `P0`

```markdown
## 描述
在现有 AutoDevColors 中添加 Xiuper 子对象，包含虚空色阶、能量色阶和信号色。
Xiuper 是 AutoDev 的特殊版本，不新建独立文件。

## 任务清单
- [ ] 在 `AutoDevColors.kt` 添加 `Xiuper` 子对象
- [ ] 更新 `AutoDevTheme.kt` 添加 Xiuper 主题模式
- [ ] 更新 TypeScript `colors.ts`
- [ ] 添加 CSS 变量定义

## 验收标准
- 虚空背景色 (#0B0E14) 应用到主界面
- accent-xiu (#00F3FF) 用于用户交互元素
- accent-ai (#D946EF) 用于 AI 生成内容
```

### Issue 2: [Animation] 实现 Xiuper 动效系统与 Launch 启动动画
**标签:** `enhancement`, `animation`, `P0`

```markdown
## 描述
实现 "Xiu" 曲线和弹簧动效，并创建品牌 Launch 启动动画。

## 动效曲线
- `ease-xiu`: cubic-bezier(0.16, 1, 0.3, 1) - 极速响应
- `ease-stream`: linear - AI 代码流式输出
- `spring-tactile`: 弹簧回弹 - 微交互反馈

## Launch 启动动画
展示 Xiuper "咻" 的品牌美学：
1. Logo 从中心快速放大 (ease-xiu, 300ms)
2. 电光青 (#00F3FF) 拖尾/光晕效果
3. "Xiuper Fast" 文字滑入 (ease-xiu, 200ms)
4. 整体淡出进入主界面 (150ms)

## 任务清单
- [ ] 创建 `AutoDevAnimation.kt` 定义动效曲线
- [ ] 实现 `XiuperLaunchScreen.kt` 启动动画
- [ ] 创建可复用的动效扩展函数
- [ ] 添加 reduced-motion 支持
- [ ] TypeScript/CSS 同步

## 验收标准
- 应用启动时展示 Launch 动画
- 动画总时长 < 800ms
- 支持 prefers-reduced-motion
```

### Issue 3: [Component] 实现幽灵文本 Ghost Text
**标签:** `feature`, `editor`, `P0`

```markdown
## 描述
实现 AI 代码补全的幽灵文本效果，支持部分采纳。

## 功能需求
- AI 预测以 40% 透明度 + 斜体显示
- Tab 全量采纳，Ctrl+→ 逐词采纳
- 采纳时 spring-tactile 弹跳效果
```

### Issue 4: [Component] 重构命令面板为全知命令栏
**标签:** `enhancement`, `ux`, `P1`

```markdown
## 描述
将现有命令面板重构为 Xiuper 风格的 Omnibar。

## 功能需求
- 宽幅悬浮设计 (750px+)
- 模糊搜索 + 权重排序
- 上下文芯片显示
- 快捷键提示
- 预览面板
```

### Issue 5: [Cleanup] 清理硬编码颜色
**标签:** `tech-debt`, `refactor`, `P2`

```markdown
## 描述
替换所有硬编码的 `Color(0x...)` 为设计系统 Token。

## 当前状态
189 处硬编码颜色，分布在 14 个文件中。

## 任务清单
- [ ] DiffSketchRenderer.kt (8处)
- [ ] PlanSummaryBar.kt (12处)
- [ ] TaskPanel.kt (5处)
- [ ] 其他文件...
```

---

## 9. 时间线总览

```
Week 1-2: Phase 1 - 基础设施升级
  ├─ AutoDevColors.kt 增强 (添加 Xiuper 子对象)
  ├─ AutoDevAnimation.kt 新建
  ├─ Launch 启动动画实现
  └─ TypeScript 同步

Week 3-4: Phase 2 - 核心组件
  ├─ Ghost Text
  ├─ Omnibar
  └─ Forge Diff

Week 5-6: Phase 3 - AI 交互
  ├─ 思维链可视化
  ├─ 动力学排版
  └─ 光标残影

Week 7-8: Phase 4 - 清理优化
  ├─ 硬编码清理
  └─ 性能优化
```

---

## 9.1 Launch 启动动画详细规格

### 视觉序列

```
[0ms]     黑屏 (bg-void #0B0E14)
          ↓
[50ms]    Logo 出现在中心，scale: 0.3, opacity: 0
          ↓
[50-350ms] Logo 放大 (ease-xiu)
           - scale: 0.3 → 1.0
           - opacity: 0 → 1
           - 伴随电光青光晕扩散
          ↓
[350-550ms] "Xiuper Fast" 文字从下方滑入 (ease-xiu)
            - translateY: 20px → 0
            - opacity: 0 → 1
          ↓
[550-700ms] 短暂停留，光晕呼吸效果
          ↓
[700-850ms] 整体淡出 (ease-xiu)
            - opacity: 1 → 0
          ↓
[850ms]   进入主界面
```

### Compose 实现参考

```kotlin
@Composable
fun XiuperLaunchScreen(onFinished: () -> Unit) {
    var phase by remember { mutableStateOf(0) }
    
    LaunchedEffect(Unit) {
        delay(50)
        phase = 1  // Logo 放大
        delay(300)
        phase = 2  // 文字滑入
        delay(200)
        phase = 3  // 停留
        delay(150)
        phase = 4  // 淡出
        delay(150)
        onFinished()
    }
    
    val logoScale by animateFloatAsState(
        targetValue = if (phase >= 1) 1f else 0.3f,
        animationSpec = tween(300, easing = EaseXiu)
    )
    
    val logoAlpha by animateFloatAsState(
        targetValue = if (phase in 1..3) 1f else 0f,
        animationSpec = tween(if (phase == 4) 150 else 300, easing = EaseXiu)
    )
    
    // ... 渲染逻辑
}

// Xiuper 专用缓动曲线
val EaseXiu = CubicBezierEasing(0.16f, 1f, 0.3f, 1f)
```

### 视觉元素

- **背景**: 虚空黑 `#0B0E14`
- **Logo**: AutoDev Logo（或 Xiuper 专属 Logo）
- **光晕**: 电光青 `#00F3FF`，模糊半径 20-40dp
- **文字**: "Xiuper Fast" 使用 Orbitron 字体，电光青色

---

## 10. 参考资源

- [Xiuper 设计系统架构报告](xuiper-ds.md)
- [当前色彩系统文档](design-system-color.md)
- [Compose 实现指南](design-system-compose.md)
- [Material 3 Color System](https://m3.material.io/styles/color/overview)
- [Linear Design System](https://linear.app/design)
- [Raycast Extension Guidelines](https://developers.raycast.com/design-guidelines)

---

**版本**: 1.0.0  
**创建日期**: 2025-12-07  
**作者**: AutoDev Team  
**状态**: Draft - 待 Review


