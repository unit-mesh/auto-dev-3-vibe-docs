# AutoDev Xiuper 3.0 - 跨平台全生命周期 AI 编程助手

> One Platform. All Phases. Every Device.

**AutoDev Xiuper 3.0.0-alpha** 正式发布！这是一款基于 Kotlin Multiplatform (KMP) 构建的 **全生命周期 AI 编程助手**，覆盖从需求分析、代码编写、文档管理到代码审查的完整开发流程，并且支持 Desktop（macOS/Windows/Linux）、CLI、VSCode、JetBrains IDE、Web、Android 和 iOS 全平台运行。

🔗 官网：[https://xiuper.com](https://xiuper.com)

---

## 💡 为什么需要 Xiuper？

传统的 AI 编程工具往往只聚焦于「代码补全」或「对话问答」单一场景。但真实的软件开发是一个复杂的生命周期：

```
需求理解 → 架构设计 → 代码实现 → 测试验证 → 文档编写 → 代码审查 → 部署运维
```

**Xiuper 的设计理念**：不仅仅是一个 Copilot，而是贯穿整个软件开发生命周期的 **AI 原生开发伙伴**。

---

## 🎯 核心能力：七大 Agent 模式

Xiuper 提供七种专业化的 Agent 模式，每种模式针对特定开发场景优化：

### 1. 💬 Chat - 轻量级对话

最简单的交互模式，无需复杂工具链，快速获取 AI 辅助：

- 代码问答与解释
- 技术方案讨论  
- 快速原型设计

### 2. 🤖 Agentic - 自主编码代理

完整的自主编码能力，拥有文件系统、终端执行、代码搜索等全套工具：

- **智能代码编辑**：基于上下文的精准代码修改（SmartEdit）
- **项目感知**：理解项目结构，自动定位相关文件
- **迭代开发**：计划 → 执行 → 验证 → 修正的完整闭环

### 3. 📊 ChatDB - 自然语言数据库交互

**这是 Xiuper 的亮点功能之一**。将自然语言转换为 SQL，让非技术人员也能查询数据库：

```
用户："帮我查询本月销售额前 10 的产品"
       ↓
ChatDB：自动生成 SQL → 执行查询 → 可视化展示
```

核心特性：
- **多数据库支持**：同时连接多个数据库，智能选择目标库
- **Schema 链接**：基于关键词的智能表/字段匹配
- **SQL 自动修正**：语法错误自动修复，最多 3 轮迭代
- **数据可视化**：查询结果自动生成图表（基于 Lets-Plot）

### 4. 📚 Knowledge - AI 原生文档阅读

**DocQL（Document Query Language）** 是 Xiuper 独创的文档查询语言，让 AI 精准定位文档内容：

```kotlin
// 查询 Markdown 文档的特定章节
$.content.h2("架构设计")

// 获取代码文件中的特定类
$.code.class("UserService")

// 从代码块中提取 Kotlin 示例
$.content.codeblock[?(@.language=="kotlin")]

// 搜索所有 Service 类文件
$.files[?(@.name~="Service")]
```

支持的文档格式：
- Markdown / Text
- PDF / DOCX / PPTX
- 源代码文件（Java、Kotlin、Python、TypeScript 等）

### 5. 🔍 Code Review - 智能代码审查

**双阶段审查流程**：

**阶段一：Walkthrough 分析**
- 理解变更意图（Why）
- 总结实现方式（What）
- 生成变更摘要和架构图（Mermaid）

**阶段二：精准修复建议**
- 基于 Linter 结果的问题定位
- 自动生成符合项目规范的修复 Patch
- 优先级排序：Error > Warning

```markdown
<!-- walkthrough_start -->
## Walkthrough

此次变更重构了用户认证模块，将单体的 AuthService 拆分为...

## Changes

| Cohort | File(s) | Summary |
|--------|---------|---------|
| **认证核心** | `AuthService.kt` | 提取令牌验证逻辑... |
<!-- walkthrough_end -->
```

### 6. 🌐 WebEdit - 浏览器交互编辑

**Xiuper 独有的创新模式**：在浏览器中选择 DOM 元素，AI 自动定位到源代码位置。

工作流程：
1. 浏览网页，使用标签功能标记感兴趣的元素
2. AI 分析 DOM 结构、CSS 选择器、组件层级
3. 自动匹配 React/Vue/Angular 组件源码
4. 直接在源文件中进行修改

核心能力：
- **DOM 元素映射**：从页面元素反向定位源代码
- **组件识别**：识别 React/Vue 组件结构
- **样式追踪**：定位 CSS/SCSS 样式定义

### 7. 🔗 Remote - 分布式代理

连接远程 mpp-server，实现团队协作和资源共享：
- 共享 LLM 配额
- 团队知识库集中管理
- 多环境统一配置

---

## 🛠️ 技术亮点

### Kotlin Multiplatform 架构

Xiuper 采用 **Kotlin Multiplatform (KMP)** 构建，实现真正的「一次编写，多处运行」：

```
mpp-core/          # 核心业务逻辑（共享代码 ~95%）
├── commonMain/    # 跨平台通用代码
├── jvmMain/       # JVM 特定实现
├── jsMain/        # JavaScript/Node.js 实现
├── iosMain/       # iOS 原生实现
└── wasmJsMain/    # WebAssembly 实现

mpp-ui/            # Compose Multiplatform UI
mpp-idea/          # JetBrains IDE 插件
mpp-vscode/        # VS Code 扩展
mpp-server/        # 远程服务端
```

**KMP 带来的优势**：
- **代码复用率 95%**：核心逻辑只需编写一次
- **原生性能**：各平台使用原生运行时
- **类型安全**：Kotlin 的强类型系统贯穿全栈

### MCP（Model Context Protocol）集成

Xiuper 原生支持 **MCP 协议**，轻松扩展工具生态：

```yaml
# ~/.autodev/mcp.json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-filesystem"]
    },
    "github": {
      "command": "npx", 
      "args": ["-y", "@anthropic/mcp-github"]
    }
  }
}
```

支持的 MCP 传输协议：
- **Stdio**：本地命令行工具
- **SSE**：远程 HTTP 服务
- **WebSocket**：实时双向通信

### 智能评分与重排序

Xiuper 内置专业的 RAG（检索增强生成）评分系统：

- **BM25 评分**：基于词频的经典相关性算法
- **RRF 融合**：多源结果的倒数排名融合
- **LLM 重排序**：AI 辅助的语义相关性判断

### TreeSitter 代码解析

基于 **TreeSitter** 的跨平台代码解析能力：

- 支持 10+ 编程语言（Java、Kotlin、Python、JS/TS、Go、Rust 等）
- 精准的 AST 级别代码理解
- 类/函数/方法的结构化提取

---

## 🚀 快速开始

### Desktop 应用

```bash
# macOS
brew install xiuper

# 或直接下载
# https://github.com/phodal/auto-dev/releases
```

### CLI 工具

```bash
npm install -g @xiuper/cli

# 启动交互式 TUI
xiuper

# 编码模式
xiuper code

# 代码审查
xiuper review
```

### VS Code 扩展

在 VS Code 扩展市场搜索 **AutoDev** 或直接安装：

```bash
code --install-extension autodev.autodev
```

---

## 📝 配置示例

### 基础配置

创建 `~/.autodev/config.yaml`：

```yaml
model:
  name: deepseek-chat
  baseUrl: https://api.deepseek.com
  apiKey: ${DEEPSEEK_API_KEY}

agent:
  maxIterations: 20
  enableStreaming: true

features:
  codeReview: true
  docQL: true
  chatDB: true
```

### 数据库配置（ChatDB）

```yaml
databases:
  production:
    type: mysql
    host: localhost
    port: 3306
    database: myapp
    username: ${DB_USER}
    password: ${DB_PASS}
    
  analytics:
    type: postgresql
    connectionString: ${ANALYTICS_DB_URL}
```

---

## 🎨 设计系统

Xiuper 拥有统一的设计系统，确保跨平台体验一致：

- **Compose**：使用 `AutoDevColors` 和 Material 3 主题
- **CLI/TUI**：使用 `semanticInk`/`semanticChalk` 语义化颜色
- **无硬编码颜色**：所有颜色通过 Design Token 管理

---

## 🔮 未来规划

### 短期（v3.0 正式版）

- [ ] iOS 原生应用完善
- [ ] Android 原生应用
- [ ] Web 在线版本
- [ ] 更多 LLM 提供商支持

### 中期（v3.x）

- [ ] 团队协作功能
- [ ] 知识图谱构建
- [ ] 自定义 Agent 工作流
- [ ] 插件市场

### 长期

- [ ] 本地模型支持（Ollama 集成）
- [ ] 多模态理解（图像、音频）
- [ ] 跨项目知识迁移

---

## 🤝 参与贡献

Xiuper 是开源项目，欢迎参与贡献：

- **GitHub**: https://github.com/phodal/auto-dev
- **官网**: https://xiuper.com
- **文档**: https://xiuper.com/docs

### 开发指南

```bash
# 克隆项目
git clone https://github.com/phodal/auto-dev.git
cd auto-dev

# CLI 开发
./gradlew :mpp-core:jsNodeProductionLibraryDistribution
cd mpp-ui && npm run build && npm run start

# Desktop 开发
./gradlew :mpp-ui:jvmRun

# IDEA 插件开发
cd mpp-idea && ../gradlew buildPlugin
```

---

## 📜 版本历史

### v3.0.0-alpha (2024-12-14)

🎉 **首个 Alpha 版本发布**

- ✨ 七大 Agent 模式全面升级
- 🌐 KMP 跨平台架构重构
- 📊 ChatDB 多数据库支持
- 📚 DocQL 文档查询语言
- 🔍 Code Review 双阶段审查
- 🌐 WebEdit 浏览器交互模式
- 🔌 MCP 协议原生支持

---

**AutoDev Xiuper** - 让 AI 陪伴你的每一行代码。

> *"One Platform. All Phases. Every Device."*

---

*Copyright © 2024 Unit Mesh. Released under MIT License.*


